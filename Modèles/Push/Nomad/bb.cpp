#include <cmath>
#include <iostream>
#include <fstream>
#include <cstdlib>
using namespace std;
//déclaration des constantes
int main ( int argc , char ** argv ) {
	//déclaration des variables
	if ( argc >= ) {
		ifstream in (argv[1]);
		for ( int i = 0 ; i < X ; i++) {
			in >> x[i];
		}
		if (in.fail() )
			f = 1e20;
		else {
			c1 = 1;
		}
		in.close();
	}
	cout << f << " " << endl:
	return 0;
}